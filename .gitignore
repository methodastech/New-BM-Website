# ── Secrets — NEVER commit ──
.env
.env.local
.env.*.local

# Node
node_modules/
npm-debug.log*
*.log

# OS / editor
.DS_Store
Thumbs.db
.vscode/

# Scratch / working backups
tmp/
scratchpad/

# Local dev server (production runs the PHP endpoints in api/toyyibpay/)
server.js
package.json
package-lock.json

# Editor / working backups — never deploy these (repo root is the web root)
*.bak
*.bak-*
*.PRE-*
*.orig
*.save
